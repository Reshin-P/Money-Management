{
  "type": "module",
  "devDependencies": {
    "nodemon": "^2.0.22",
    "offline": "nodemon --exec 'serverless offline start' --ext js,json,yml"
  },
  "dependencies": {
    "aws-sdk": "^2.1691.0",
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "uuid": "^10.0.0"
  },
  "scripts": {
    "start": "nodemon --exec serverless offline"
  }
}
